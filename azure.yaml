# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: azd-template
metadata:
  template: vanilla-js
services:
  web:
    project: .
    language: js
    host: appservice
    resourceName: azd-template-kikkoust
    docker:
      enabled: false